// FIX: The reference to "vite/client" was causing a type resolution error.
// Since no Vite-specific environment variables (import.meta.env) are used in the project,
// this line was removed to resolve the build error.
